{
  "frontend/src/App.tsx": "Main application component that renders the Header, MusicEditor, and Footer in a full-height flex column layout with a background color token.",
  "frontend/src/types/music.ts": "TypeScript type definitions for musical elements including Note, Composition, FingeringPattern, FingeringConfiguration, OctaveRange, and newly added PresetSong interface and SongCategory union type for the preset songs feature.",
  "frontend/src/utils/ocarinaNoteMapper.ts": "Utility mapping notes to fingering patterns and frequencies; `getNoteFingerPattern` now resolves patterns using relative chromatic degree (note name without octave) so the same pattern is returned for any octave, with 'C_upper' handling the top-of-span C.",
  "frontend/src/utils/audioSynthesis.ts": "Web Audio API utility that creates synthetic ocarina sounds using oscillators, filters, and ADSR envelopes.",
  "frontend/src/utils/compositionSerializer.ts": "Serialization utilities to convert Composition objects to/from Uint8Array for backend storage.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; `useGetFingeringDefaults`, `useSetFingeringDefaults`, and `useResetFingeringDefaults` now accept an `isAuthenticated` boolean parameter and are disabled (or throw) when the user is not authenticated, preventing any fingering backend calls for unauthenticated users.",
  "frontend/src/hooks/useComposition.ts": "Custom hook managing composition state; now imports `useInternetIdentity` to derive `isAuthenticated`, passes it to `useGetFingeringDefaults` so fingering defaults are only fetched when logged in, resets the seeded flag on logout, and exposes `isAuthenticated` to consumers.",
  "frontend/src/hooks/useAudioPlayback.ts": "Hook managing audio playback with Web Audio API; now supports play (with optional repeat), pause (suspends AudioContext and stores position), resume (restarts scheduling from paused note index), and stop (resets fully), exposing isPlaying, isPaused, pause, resume, and stop.",
  "frontend/src/components/Header.tsx": "Application header displaying the Ocarina Composer title and subtitle with a music icon.",
  "frontend/src/components/Footer.tsx": "Footer component with attribution link to caffeine.ai and copyright notice.",
  "frontend/src/components/MusicEditor.tsx": "Main editor component that now includes a Song Library button in the toolbar and conditionally renders GuidedPlaybackView when a preset song is selected, or the normal composition editor with all existing controls when in standard mode.",
  "frontend/src/components/PianoKeyboard.tsx": "Extended piano keyboard spanning C3-C7 (4+ octaves) with horizontal scrolling; keys within the active ocarina range are highlighted in amber while keys outside remain white/dark but fully playable.",
  "frontend/src/components/SheetMusic.tsx": "Sheet music display component rendering individual staff lines for each note with vertical wrapping behavior using flex-wrap, matching the OcarinaTablature's responsive layout pattern.",
  "frontend/src/components/OcarinaTablature.tsx": "Traditional 4-hole ocarina tablature display; resolves each note's fingering via `getNoteFingerPattern` with relative key lookup so notes in any octave display the correct shared pattern, with a subtitle noting the config applies to all octaves.",
  "frontend/src/components/OcarinaIllustration.tsx": "SVG illustration of a 4-hole ocarina that resolves the active note's fingering via `getNoteFingerPattern` with relative key lookup, so the same hole pattern displays for any octave; shows the active octave range in the caption.",
  "frontend/src/components/LyricsEditor.tsx": "Component for adding and editing lyrics aligned with musical notes.",
  "frontend/src/components/NoteDurationSelector.tsx": "Button group for selecting note durations (whole, half, quarter, eighth, sixteenth) with musical symbols.",
  "frontend/src/components/PlaybackControls.tsx": "Unified playback bar component containing play/stop, pause/resume, repeat toggle, save, and load buttons in a single flex row with tooltips; reflects isPaused and isRepeat states visually.",
  "frontend/src/components/SaveCompositionDialog.tsx": "Dialog for saving compositions (no login required); after a successful save it transitions to a success state displaying the generated composition ID with a copy-to-clipboard button so users can retrieve their work later.",
  "frontend/src/components/LoadCompositionDialog.tsx": "Dialog for loading saved compositions (no login required); shows the full list of saved compositions with titles, descriptions, and numeric IDs, and also allows loading by entering a composition ID directly.",
  "frontend/src/lib/utils.ts": "Utility function combining clsx and tailwind-merge for conditional className handling.",
  "frontend/src/index.css": "Global styles with OKLCH color system using warm earth tones (browns, ambers, creams) for a musical aesthetic.",
  "frontend/tailwind.config.js": "Tailwind configuration extending theme with custom warm color palette and Inter font family.",
  "frontend/src/main.tsx": "Application entry point that wraps the app with QueryClientProvider and InternetIdentityProvider, and mounts the React root with global CSS and Sonner toast support.",
  "frontend/src/utils/defaultFingeringPatterns.ts": "Generates a single shared default 4-hole ocarina fingering configuration with 13 entries keyed by relative note name (no octave suffix); the `getDefaultFingeringPatterns()` function takes no parameters and is octave-independent.",
  "frontend/src/components/FingeringConfigEditor.tsx": "Fingering configuration editor showing exactly 13 rows labeled with relative note names (no octave numbers: C, C#, ..., B, C upper); editing any pattern immediately affects tablature and illustration for all octaves.",
  "frontend/src/components/OcarinaSettingsDialog.tsx": "Settings dialog that now accepts an `isAuthenticated` prop and returns `null` (no DOM trace) when the user is not logged in; the inner `AuthenticatedSettingsDialog` component uses inline success/error state messages instead of toasts for save and reset feedback, and the save button is labeled \"Save Fingering Config\".",
  "frontend/src/data/presetSongs.ts": "Data file defining all 15 preset songs (3 Classic Folk, 6 Video Games, 6 Mixed Genres) as structured note sequences with title, category, tempo, and Note arrays; exports ALL_PRESET_SONGS array, SONGS_BY_CATEGORY grouped object, and CATEGORY_ICONS map.",
  "frontend/src/hooks/useGuidedPlayback.ts": "Custom hook managing guided step-by-step playback state for a PresetSong, including current step tracking, play/pause/stop/next/previous controls, recursive setTimeout-based auto-advance at the song's BPM tempo, and audio synthesis integration via createOcarinaSound.",
  "frontend/src/components/SongLibraryDialog.tsx": "Dialog component displaying all 15 preset songs organized by collapsible category sections (Classic Folk, Video Games, Mixed Genres) with song title, note count, and BPM; selecting a song invokes the onSelectSong callback and closes the dialog.",
  "frontend/src/components/NoteSequenceGuide.tsx": "Scrollable note sequence list component showing all notes in a preset song with distinct styling for completed (dimmed), active (highlighted), and upcoming notes; each entry shows step number, note name, duration, and a 2×2 mini hole diagram; auto-scrolls the active note into view.",
  "frontend/src/components/GuidedPlaybackView.tsx": "Full guided playback UI component displaying the selected song's title, progress bar, current note name prominently, OcarinaIllustration with live fingering, playback controls (Play/Pause/Stop/Prev/Next), and the NoteSequenceGuide; uses useGuidedPlayback hook for all state management."
}